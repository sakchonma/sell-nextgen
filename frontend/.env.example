# ------------------------------------------------------------------------------
# NEXTGEN Frontend Environment Variables Template (Vite Prefix Required)
# ------------------------------------------------------------------------------

# Optional: Override the Backend API Base URL if running on a different domain or port.
# If not set, it will default to proxying via Vite configuration (/api).
VITE_API_URL=http://localhost:5001
